apiVersion: snapshot.storage.k8s.io/v1
kind: VolumeSnapshot
metadata:
  name: mysql-volume-snap-0
  namespace: mysql
spec:
  volumeSnapshotClassName: fsx-snapclass
  source:
    persistentVolumeClaimName: mysql-store-mysql-set-0

--- 

apiVersion: snapshot.storage.k8s.io/v1
kind: VolumeSnapshot
metadata:
  name: mysql-volume-snap-01
  namespace: mysql
spec:
  volumeSnapshotClassName: fsx-snapclass
  source:
    persistentVolumeClaimName: mysql-store-mysql-set-1

--- 

apiVersion: snapshot.storage.k8s.io/v1
kind: VolumeSnapshot
metadata:
  name: mysql-volume-snap-02
  namespace: mysql
spec:
  volumeSnapshotClassName: fsx-snapclass
  source:
    persistentVolumeClaimName: mysql-store-mysql-set-2

--- 

apiVersion: snapshot.storage.k8s.io/v1
kind: VolumeSnapshot
metadata:
  name: mysql-volume-snap-03
  namespace: mysql
spec:
  volumeSnapshotClassName: fsx-snapclass
  source:
    persistentVolumeClaimName: mysql-store-mysql-set-3

